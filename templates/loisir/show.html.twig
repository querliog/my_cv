{% extends 'base.html.twig' %}

{% block title %}Loisir{% endblock %}

{% block body %}
    <h1>Loisir</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ loisir.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('loisir_index') }}">back to list</a>

    <a href="{{ path('loisir_edit', {'id': loisir.id}) }}">edit</a>

    {{ include('loisir/_delete_form.html.twig') }}
{% endblock %}
